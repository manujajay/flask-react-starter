import React from 'react';
import ReactDOM from 'react-dom';
import LandingPage from './App'; // Make sure the path is correct

ReactDOM.render(
  <React.StrictMode>
    <LandingPage />
  </React.StrictMode>,
  document.getElementById('root')
);
